#include "../library/fast.hpp"

class a {
public:
	void solve(std::istream& cin, std::ostream& cout) {
      int x = 1;
      VI a(3,122);
      debug() << imie(a);
      while (x--){
        cout << "hi\n";
      }
	}
};
